
class Solution {
    int solve(int a, int b, int c) {
        if( a> (b + c)*2 + 2 || b> (a + c)*2 + 2 || c> (b + a)*2 + 2) {
            return -1;
        }
        
        return a + b + c;
    }
};
